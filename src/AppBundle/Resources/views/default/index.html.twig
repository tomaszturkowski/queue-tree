{% extends 'AppBundle::base.html.twig' %}

{% block content %}
    <div class="content-section-a">
        <div class="container">
            <div class="row">
                {{ form(switching_project_form) }}
                {{ form(switching_problem_form) }}
            </div>
            <div class="row">
                <div class="col-lg-6 col-sm-6">
                    <h3>New Project</h3>
                    {{ form(project_form) }}
                </div>
                <div class="col-lg-6 col-sm-6">
                    <h3>New Problem</h3>
                    {{ form(problem_form) }}
                </div>
            </div>
        </div>
    </div>
{% endblock content %}
